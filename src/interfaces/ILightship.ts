/** A JSON definition for a Lightship. */
export interface ILightship {
    /** e.g. 'The Brightside' */
	name: string;
	/** e.g. 'brightside' */
	id: string;
	/** e.g. 'img/ships/whaleship.png' */
	token: string;
	/** e.g. '#000' */
	themeColor: string;
}